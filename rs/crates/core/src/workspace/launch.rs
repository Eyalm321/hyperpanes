//! Port of `resolveLaunchWorkspace` / `getInitialWindows` from `src/main/workspace.ts`
//! (resolve the workspace to launch; fall back to `last-workspace.json`, then to a bare
//! window). Uses `persistence::paths` + `workspace::io`.
//!
//! STUB — owned by track `persistence-cli`.
