//! Port of `src/main/ansi-strip.ts` — strip ANSI escape sequences (CSI, OSC, the
//! two-byte ESC forms) to clean text. Mirror every case in `ansi-strip.test.ts`.
//!
//! STUB — owned by track `core-text`. Replace this with the 1:1 port + `#[test]`s.
