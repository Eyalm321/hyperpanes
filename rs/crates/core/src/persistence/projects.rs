//! Port of `src/main/projects.ts` — git-project history (`projects.json`): `canonicalPath`
//! (Windows drive-letter uppercase + backslash normalization), the 8-color hash palette,
//! `gitRepoName`, and upsert-on-cwd-change. A repo must keep its color across restarts
//! (and across the TS→Rust switch). Mirror `projects.test.ts`.
//!
//! STUB — owned by track `persistence-cli`.
