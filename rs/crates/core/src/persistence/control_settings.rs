//! Control-server settings persistence — `{ enabled, allowInput }` in
//! `control-settings.json` under the userData dir (default both false). Atomic write.
//!
//! STUB — owned by track `persistence-cli`.
